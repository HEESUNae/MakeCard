import React from 'react';
import { StyledHeader } from './style';

const Header: React.FC = () => {
  return (
    <StyledHeader>
      <h1>나만의 카드 만들기</h1>
    </StyledHeader>
  );
};

export default Header;
